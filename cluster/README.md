# Redis-Cache-Docker Clusterizado

Para subir uma versão no docker-compose do Redis-Cache Clusterizado.

Execute o comando para gerar a imagem:

```
  cd cluster

  docker-compose build

  # Em seguida

  docker-compose up -d
  
```